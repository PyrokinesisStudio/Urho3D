<renderpath>
    <!-- VELOCITY -->
    <rendertarget name="velocity" sizedivisor="1 1" format="rg16f" />
    <command type="clear" color="0.5 0.5 0.5 1" depth="1.0" output="velocity" />
    <command type="scenepass" pass="velocity" output="velocity" />

    <!-- STANDARD FORWARD RENDERING -->
    <command type="clear" color="fog" depth="1.0" stencil="0" />
    <command type="scenepass" pass="base" vertexlights="true" metadata="base" />
    <command type="forwardlights" pass="light" />
    <command type="scenepass" pass="postopaque" />
    <command type="scenepass" pass="refract">
        <texture unit="environment" name="viewport" />
    </command>
    <command type="scenepass" pass="alpha" vertexlights="true" sort="backtofront" metadata="alpha" />
    <command type="scenepass" pass="postalpha" sort="backtofront" />

    <!-- MOTION BLUR -->
    <command type="quad" vs="MotionBlur" ps="MotionBlur" output="viewport">
        <texture unit="diffuse" name="viewport" />
        <texture unit="environment" name="velocity" />
        <parameter name="Samples" value="50" />
    </command>   
</renderpath>
